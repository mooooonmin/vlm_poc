# Python 가상환경은 로컬 실행 환경이므로 저장소에 포함하지 않습니다.
.venv/

# Python 실행 중 생성되는 캐시 파일은 재생성 가능하므로 제외합니다.
__pycache__/
*.py[cod]

# 로컬 환경 변수 파일은 토큰이 포함될 수 있으므로 제외합니다.
.env

# 운영 중 생성될 수 있는 로그 파일은 제외합니다.
*.log

# 영상 업로드, 다운로드, 추출 프레임 등 PoC 임시 산출물을 제외합니다.
tmp/
